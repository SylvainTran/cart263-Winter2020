// Unit test for scene linking
//
// Checking out if scenes are appropriately unpaired after breaking out of snapped state
describe('resetPlayer(scene, controller)', () => {
    it('resets the data of player entered paired scenes',
        ()=> {
            let controller = game.scene.getScene('Controller');
            let mockDraggable;
            let mockScene = new CriticalHit('MockScene')
            // WIP
    });
});