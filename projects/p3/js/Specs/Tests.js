// Unit tests
//
// WIP
describe('interestingTest(scene, controller)', () => {
    it('WIP',
        ()=> {
            let controller = game.scene.getScene('Controller');
            // WIP
    });
});